<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardElevation="4dp"
    app:cardCornerRadius="16dp"
    android:foreground="?android:attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp">

            <!-- Imagen del producto -->
            <ImageView
                android:id="@+id/itemProductoImageView"
                android:layout_width="match_parent"
                android:layout_height="140dp"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_launcher_foreground" />

            <!-- Precio y disponibilidad -->
            <TextView
                android:id="@+id/itemPrecioTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="S/ 1,580 | 12 UND."
                android:textColor="@android:color/holo_red_dark"
                android:textStyle="bold" />

            <!-- Nombre del producto -->
            <TextView
                android:id="@+id/itemNombreTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:ellipsize="end"
                android:maxLines="2"
                android:text="COMPUTADORA KENYA GENWORK D70S"
                android:textSize="14sp" />

            <!-- Botón "Ver más" -->
            <Button
                android:id="@+id/buttonVerMas"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:minHeight="0dp"
                android:drawableLeft="@drawable/ic_visibility"
                android:paddingVertical="0dp"
                android:drawablePadding="1dp"
                android:padding="1dp"
                android:text="Ver más"
                android:textSize="10dp"
                android:textColor="@android:color/white" />
        </LinearLayout>
        <!-- Botón de corazón -->
        <ImageButton
            android:id="@+id/buttonFavorito"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:scaleType="fitCenter"
            android:layout_alignParentStart="true"
            android:layout_margin="8dp"
            android:background="@android:color/transparent"
            android:src="@drawable/corazon21"
            android:contentDescription="Agregar a seleccionados" />


    </FrameLayout>
</androidx.cardview.widget.CardView>
